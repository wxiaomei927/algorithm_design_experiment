# 项目依赖说明

## 编译工具
- GCC 编译器 (版本 4.8 或更高)
- Make 工具

## 运行环境
- Linux 或 Windows (通过 MinGW 编译)

## 外部依赖
- 无额外外部依赖

## 图表生成
图表生成功能建议使用 Python 配合 matplotlib 库进行后处理分析。

### Python 依赖（可选）
```
matplotlib >= 3.0
pandas >= 1.0
```